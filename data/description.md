# "data" Description

This directory contains the data used for training and prediction.

The `ForModelTraining` directory contains the datasets used for training the models. The `ForModelPrediction` directory contains the datasets used for prediction.
The `RawImages` directory contains the raw imagery data.

